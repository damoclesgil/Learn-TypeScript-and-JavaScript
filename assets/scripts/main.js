class Greeter {
    sayHello() {
        console.log("Hello" + name);
    }
}
function sortByName(a) {
    var result = a.slice(0);
    result.sort(x, y);
    {
        return x.name.localeCompare(y.name);
    }
    ;
    return result;
}
sortByName([{ name: "Dâmocles", age: 20 }]);
